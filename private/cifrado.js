const codificar = (letras, texto, num) => {
    var texto_codificado
    var texto = texto.toUpperCase()
    var caracter

    for (let i = 0; i < texto.length; i++) {
        const element = array[i];
        
    }
}